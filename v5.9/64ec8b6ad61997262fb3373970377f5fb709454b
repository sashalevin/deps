64ec8b6ad619 ("ftrace: Choose RCU Tasks based on TASKS_RCU rather than PREEMPTION")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
