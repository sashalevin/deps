952c48b0ed18 ("rbd: call rbd_dev_unprobe() after unwatching and flushing notifies")
0e4e1de5b63f ("rbd: avoid a deadlock on header_rwsem when flushing notifies")
b9ef2b8858a0 ("rbd: don't establish watch for read-only mappings")
