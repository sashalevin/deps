448e9f34d91d ("rcu: Standardize explicit CPU-hotplug calls")
2cb1f6e9a743 ("rcu: Conditionally build CPU-hotplug teardown callbacks")
c964c1f5ee96 ("rcu: Assume rcu_report_dead() is always called locally")
