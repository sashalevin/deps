53bc516ade85 ("x86/msr: Move ARCH_CAP_XAPIC_DISABLE bit definition to its rightful place")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
99c84311e35f ("x86/selftests: Skip the tests if prerequisites aren't fulfilled")
