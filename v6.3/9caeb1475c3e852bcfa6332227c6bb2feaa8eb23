9caeb1475c3e ("net/mlx5: Use devlink port pointer to get the pointer of container SF struct")
9277649c66fe ("devlink: pass devlink_port pointer to ops->port_del() instead of index")
c496daeb8630 ("devlink: remove duplicate port notification")
