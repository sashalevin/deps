a835b784ba1a ("drm/dp: Add support to indicate if sink supports AS SDP")
0d024974014f ("drm/dp: add an API to indicate if sink supports VSC SDP")
47f419e07111 ("drm/dp: move intel_dp_vsc_sdp_pack() to generic helper")
