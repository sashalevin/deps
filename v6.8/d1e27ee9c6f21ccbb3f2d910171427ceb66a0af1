d1e27ee9c6f2 ("tracing: Return -EINVAL if a boot tracer tries to enable the mmiotracer at boot")
d33b10c0c73a ("tracing: Switch trace.c code over to use guard()")
180e4e390978 ("tracing: Add snapshot refcount")
