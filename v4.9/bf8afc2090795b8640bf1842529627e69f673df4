bf8afc209079 ("drm/sis: use idr_init_base() to initialize dev_priv->object_idr")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
bd8408e69a47 ("drm/nouveau/bios: set info only when the return is not 0")
