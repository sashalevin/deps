f574751cdfac ("leds: trigger: netdev: Skip setting baseline state in activate if hw-controlled")
7df1f14c04cb ("led: trig: netdev: Fix requesting offload device")
0316cc5629d1 ("leds: trigger: netdev: init mode if hw control already active")
d1b9e1391ab2 ("leds: trigger: netdev: Use mutex instead of spinlocks")
