42c4e97e06a8 ("selinux: don't use make's grouped targets feature yet")
4ce1f694eb5d ("selinux: ensure av_permissions.h is built when needed")
bcab1adeaad4 ("selinux: fix Makefile dependencies of flask.h")
